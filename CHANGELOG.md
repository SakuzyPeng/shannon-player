# 更新日志

本项目的所有重要变更都会记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增（播放会话）
- **队列、当前曲目与播放位置跨重启保留**：此前关掉应用一切归零，下次打开又从曲库第一首开始。现在退出时落盘、启动时恢复，按下播放从上次的位置续上（恢复本身**不自动播放**——启动即出声是没人要的行为）。
  - **只存曲目 ID，不存曲目副本**：曲目信息的权威在曲库。存副本会让用户改完元数据重启后队列里还是旧标题，而他刚刚才改过；顺带省下整库入队时抄一遍曲库的体积，文件删除或重扫后 ID 变化时也能自然剔除。当前曲目失效时退到它之后仍存在的第一首，而不是回到队首——上次听到队列中段，重启后从头开始比「少了一首」更突兀。
  - 随机顺序存的是**下标排列**而非曲目 ID：队列允许同一首歌多次入队，用 ID 表达顺序会有歧义。
  - 位置按 5 秒节流写盘（每秒 5 个进度事件，跟着写等于把 SSD 当秒表用），退出时补写一次——「听到一半关掉」正是最常见的退出方式。
  - 后端只负责原子存取一段文本，结构与版本号由前端拥有：会话是前端拥有的状态，后端在其中没有领域判断可做。

### 新增（播放引擎接入界面）
- **播放引擎接进 Tauri 与前端，界面终于能出声**：此前引擎只能经 CLI 试听，界面上的播放条走的是每 100 ms 自加的占位时钟——与实际发声毫无关系。现在播放 / 暂停 / 上一首 / 下一首 / seek / 音量全部经 IPC 打到引擎，进度由引擎按**输出回调已消费的帧数**上报（并扣除设备延迟），这是唯一能跟发声对齐的口径。
  - 契约类型（`PlayerEvent` / `PlaybackFormat` / `PlaybackError`）定义在 `shannon-audio` 并经 ts-rs 导出到 `src/types/generated/player.ts`，与 `ScanProgress` 放在 `shannon-core` 同理——**契约属于产生它的那一层**；放进外壳会让本该薄的它承担领域建模，而那个定义再也无法被无头测试覆盖。
  - 每个事件都带 `trackId`：引擎只认文件路径，前端队列以曲目 ID 为键。快速连点两首歌时前一首的进度事件会晚于后一首的装载到达，不盖章就会记到新曲目头上，表现为进度条跳一下。
  - 引擎**懒起**：到第一次真要放东西时才创建并打开输出设备。在 `setup` 里就建好等于应用一启动就占用声卡，而用户可能只是想整理曲库。
  - 浏览器预览（无 Tauri）回落到 `MockEngine`：不出声，但把状态机与时钟完整跑一遍。没有它则进度条、切歌、循环模式在 `pnpm dev` 里全是死的，而这些恰恰是界面开发最需要反复看的交互。
  - **随机播放改为洗牌**：此前 `shuffle` 只是个布尔值，`next()` 永远顺序推进——开关是摆设。现在存一份洗好的顺序并把当前曲目固定在首位（用户按的是「之后随机」，不是「立刻换一首」）。每次随机取一首的做法会重复会漏，一个 10 首的队列放到第 10 首时仍有约 35% 的曲目一次没放过。
  - **播放失败不自动跳下一首**，停下并按错误类别给出可操作的提示（找不到文件 / 格式不支持 / 设备被占用）。自动跳过在整库格式不支持时会变成一场无声的快进，用户完全不知道发生了什么。

### 修复
- **扫描不再递归进 macOS 包目录**：`.bundle` / `.logicx` / `.band` / `.app` / `.musiclibrary` 这类包在 Finder 里显示为单个文件，用户并不认为里面有音乐。实测 `~/Music` 除 954 个音乐文件外，另有 56 个音频躺在 `Logic Pro Library.bundle` 的采样库里（单周期波形只有几毫秒、打击乐短音），`.logicx` 工程包里还有母带片段——把它们收进曲库，与把 `.app` 里的提示音收进来是同一类错误。候选文件数由 1010 降至 944。用户**显式指定**为扫描根的包仍照扫：想把采样包当曲库是他的自由。
- **曲库到位后不再把演示曲目摆在播放条上**：一个已经扫描过的用户开门见到的仍是种子演示曲目，按下播放得到的是「这是演示曲目」——他明明有音乐，播放器却说他没有。现在曲库恢复后整库入队（不自动播放）。只入第一首会让「下一首」原地打转，按钮亮着却什么都不发生。
- **诊断工具可指定输出设备**（`play --device 名字`）：系统默认输出可能是蓝牙耳机、虚拟声卡或聚合设备，此时引擎一切正常——欠载为零、位置照常推进——而人耳什么也听不到。此前排除这个伪故障的唯一手段是改系统设置，那会同时影响其它应用，动作比问题本身还大。匹配不到时报错并列出现有设备，**不回落默认设备**：诊断工具一旦允许静默回落，结论就不再可信。

### 新增（播放引擎）
- **新增 `shannon-audio` crate，打通立体声播放路径**：ALAC / AAC / FLAC / MP3 / WAV 经 Symphonia 解码 → 声道适配 → 无锁 SPSC 环形缓冲 → CPAL 共享输出，含播放 / 暂停 / seek / 音量。与 `shannon-core` 同样零 Tauri 依赖，因此 seek 等价性、欠载计数这些验收项能在无声卡环境 `cargo test`。
  - 格式主力定为 **ALAC 而非 FLAC**：实测真实曲库 1362 首里 ALAC 占 87.5%、FLAC 为 0，照原计划只做 FLAC/MP3/WAV 会出现「阶段做完却一首曲库歌曲都放不了」。
  - **做不到的事一律报明确的错误，不静默降级**：多声道不走这条路（见下），遇到就带着布局描述报错，而不是猜个下混系数把声场弄乱。
  - **多声道整体交给系统，应用不做下混**：理由与「不自己双耳化」是同一条——自行下混等于把一条本可被系统空间化的多声道流提前拍扁，系统只看到两条声道，AirPods 上的空间音频开关会显示「不可用」。附带两项好处：系统掌握端点特性（耳机 / 扬声器 / HDMI 各不相同）而应用只能猜一套通用系数；以及用户在系统播放器里听到的下混结果，在这里应当是同一个。因此 CPAL 后端只承担立体声，一切多声道（不只是空间内容，普通 5.1 也一样）走平台原生后端——下混与空间化都依赖布局标签，而 CPAL 的配置表达不了布局。引擎里因此没有、将来也不会有下混器。
  - 声道布局与**布局来源**（码流明写 / 按声道数推断 / 未知）自解码起点就进类型：下混系数依赖布局，而 PCM 一旦进了环形缓冲就再没有源头信息，事后补录代价太大。3 声道以上一律不猜——6 声道可能是 5.1 也可能是 6.0。
  - 播放位置 = 输出回调累计消费帧数 − 设备延迟，不用定时器估算（实测共享模式延迟 615 帧 / 14 ms，不补偿会让歌词逐字高亮系统性偏早）。
  - 暂停是「斜坡到零后停止消费、但继续向设备写零帧」，不拆输出流：直接停掉会让恢复播放经历设备重启延迟，也让基于消费帧数的位置时钟失去参照。音量变化带 15 ms 斜坡，否则有可闻爆音。
  - seek 的缓冲清空**由消费端执行**：生产端把读下标推到写下标看似更省事，但消费端可能正拷着数据并随后提交下标，把重置覆盖掉——结果是旧音频继续发声，正是 seek 最不能出的毛病。等待回执超时也不得由生产端代写读下标：超时只能说明回调没有及时响应，不能证明它已经停止；此时关闭输出链并报明确错误。
  - seek 引起的重缓冲**不计入欠载**：这项指标要回答「实时性够不够」，把用户拖进度条的次数混进去就失去了诊断价值。
- **采样率不匹配时自动重采样**：默认输出设备不一定支持源采样率——实测本机默认设备只提供 24 与 48 kHz，而曲库主力是 44.1 kHz 的 ALAC，此前的「不匹配就报错」等于一首歌都放不了。重采样因此不是锦上添花而是可用性前提，随立体声路径一起做掉。
  - 用 rubato 的同步 FFT 重采样器而非异步 sinc：播放期的比率是**固定**的（44.1 → 48 kHz 就一直是这个比），同步重采样器为此而设计，可变比率的能力在这里用不上、只是白付插值代价。实测开销 1.2%（release 下播 10 秒耗 0.12 秒 CPU）。
  - 采样率**允许协商，声道数不允许**：声道数不符会静默丢声道；采样率不符则插入重采样并在 stats 里如实标记（「bit-perfect」一类的措辞必须有据可依）。挑选偏好依次是「与源一致 > 源的整数倍 > 高于源的最小者 > 最大者」——44.1 → 88.2 的滤波器比 → 48 简单，相位与混叠都更好。
  - 位置计数一律记**输出域**的帧：`seek` 返回的是源域帧位置，混用会让进度按比率走偏，44.1 → 48 kHz 时快 8.8%，拖到 1:00 会显示成 1:05。
  - 能力协商与打开输出流拆成两步（预演协商不碰设备）：环形缓冲容量、重采样比率、位置计数的时基都要按协商结果搭，先打开再回头调整意味着缓冲刚建好就得推倒重建。
- **格式矩阵测试**：此前自动化测试只覆盖 WAV（纯 Rust 能现造的只有 PCM 语料），而引擎启用着一整串解码器——承诺面比验证面大得多，ALAC / AAC / FLAC / MP3 全靠手动试听的记忆撑着。改为用 ffmpeg 从同一份扫频源转出整个格式矩阵（`--example make_corpus`），验证四件事：规格读取、**无损格式与源逐样本一致**（对齐后偏移为 0）、有损格式整体能量守恒、每种格式端到端播完且零欠载。另有一条混合格式「歌单」用例，覆盖换曲时输出流、环形缓冲与重采样器的整套重建。
  - 语料**不入库**（可复现，几 MB 二进制进 git 无意义），缺失时用例整体跳过并打印生成命令：CI 未必装了 ffmpeg，而 PCM 路径已由现造的语料覆盖。
  - 用 ffmpeg 而非 macOS 的 `afconvert`：后者只能在一个平台上生成，等于把验证能力绑死在开发者的机器上。
  - 顺带启用 AIFF 与 CAF 容器——有了可复现的语料，「开一个 feature」的成本才只剩编译体积；在此之前它是一句无法验证的承诺。
- **不同格式的同一首歌不再被当成重复折叠掉**：折叠判据此前只看「标题 / 轨位 + 时长」，于是同一段音乐的 FLAC、ALAC、MP3 各一份会被认成副本——实测 11 种编码放进同一目录，扫出来只剩 4 首。三条判据现在都额外要求音频规格一致（容器 / 编码族 / 编码 / 采样率 / 位深 / 声道数 / 权威声道掩码 / 空间音频标记）。因此同为 6 声道的 5.1 与 6.0、普通 E-AC-3 与 E-AC-3/JOC 也会分别保留。分界在于用户知不知情：导入工具留下的副本是字节级拷贝、规格必然相同，仍会照常折叠；而同时留着不同版本的人是有意为之，折叠等于替他做了没同意的删除。
- **读不到标签的合法音频不再从曲库里消失**：`lofty` 读不了 CAF，而那里是硬失败，一个合法文件只体现为「解析失败 +1」，用户看到的是「文件明明在，曲库里找不到」。改为降级——标签留空、规格与时长由 symphonia 顶上、`probe_notes` 记 `tags:unreadable`；只有标签与规格双双读不出才判定不是音频，垃圾文件仍会被拒。扫描白名单同时补上 `caf`，探测器版本升至 2，让旧缓存能识别本轮能力变化。
- 专辑详情页的音轨号缺失时显示 `·`，不再退回碟内序位：序位是个会撞号的哨兵值，同一张专辑里只要有几首缺标签，编出来的号就会与其它曲目的真实音轨号重号（实测同时出现两个「10」和两个「11」）。
- **多格式实测歌单**：自动化断言证明不了「听着对」。`--example make_playlist` 把一首真实音乐转成 11 种格式，`--example play` 传目录即连续播放。五种无损应当听不出差别；44.1k 与 48k 两份无损的对比就是重采样质量（设备工作在 48k 时前者走重采样、后者直通）；有损那几首用来听开头结尾有无咔哒。歌单不入库（含真实音乐，且可复现）。
- 修复换曲后单曲消费帧数统计恒为 0：`frames_consumed` 被同时当作「播放位置」和「累计消费量」，而装载新曲会把位置归零，于是靠前后差值算出的单曲用量被抹平。两者拆成独立计数器——位置每首归零，累计跨曲目单调递增。
- 修复输出回调生命周期与错误状态：共享状态在 `open` 前先进入暂停/重缓冲，启动时的空 ring 不再被误记为欠载；源数据全部入 ring 后显式标记自然收尾，最后不足一个回调的补零不算欠载，而播放中的部分补零会如实计数。CPAL 的设备断开、被独占等运行期错误现在经 `OutputBackend` 回到控制线程，关闭链路并进入 `Error`，不再无声却仍显示 `Playing`。
- 修复“声道数相同就直接搬运”的错误：直通现在同时要求权威声道掩码一致，`FC + LFE` 这类非常规双声道不会被当成立体声送到左右声道。
- 开发工具：`cargo run -p shannon-audio --example play -- <文件>`（试放并打印规格、协商结果、是否重采样、位置与欠载）、`--example devices`（列设备支持的声道与采样率）。
- 落地时踩到的三处，已记进文档与测试：① 单声道的判据必须是**声道数**而非掩码位置——Symphonia 的 WAV 读取器把单声道标成 `FRONT_LEFT` 而非 `FRONT_CENTER`，按掩码比对会拒播最简单的文件；② seek 后的位置换算必须**走整数**，先转秒再乘采样率会因浮点截断差一帧；③ 验证位置的语料**不能用定频正弦**——440 Hz 每 100 帧重复一次相位，上面那个 off-by-one 就被伪装成了「误差为零」，换扫频信号才暴露。
- 尚未接入 Tauri 与前端，播放条走的仍是占位时钟。

### 新增（专辑封面）
- 扫描时提取内嵌封面，生成 128 / 512 / 1024 三档缩略图；界面各处（专辑网格与列表、专辑与歌手详情、歌曲页、搜索、收藏、队列、播放条、歌词页、歌单拼贴）按显示尺寸挑档加载，不再一律占位渐变。
- **非正方形封面完整显示，不裁剪**：实测约四成封面是 1300×910、710×1000 这类宽幅或竖幅插画，裁成正方形会切掉画面主体。改为「原图居中 + 同图放大模糊填充四周」合成正方形，一个像素都不丢。
  - 合成在后端一次性完成而非前端 CSS 双层模糊：网格会同时显示几十张封面，每张挂一个模糊图层要每帧重算。实测滚动零掉帧（中位帧 8.3ms、最长 9.4ms）。
  - 宽高比差 2% 以内直接当正方形；合成前用整图中位色打底，避免圆角处露出突兀边。
- 缩放策略：**只缩小不放大**（原图比档位小就按原尺寸存，放大交给 GPU）；缩小用 Lanczos3 并逐档接力（原图 → 1024 → 512 → 128），避免大比例缩放时滤波核开销爆炸。
- 封面按**内容指纹**去重：939 首实测只对应 28 张唯一封面，整库封面处理约 1 秒；重扫时已生成的直接跳过解码。解码失败如实计数上报，对应曲目回落占位渐变而非中断扫描。
- 占位渐变始终生成：图未到位、无内嵌封面、文件损坏三种情况自动回落，调用点不写错误分支。
- 修复浅色封面四角露出深色弧线：封面图只继承了圆角半径没继承 `corner-shape`，标准圆角比容器的 superellipse 在四角多切一块，底下的占位渐变透了出来。
- 开发便利：dev 构建把 store 挂到 `window.__shannon`，配合 `warm_cache` 导出的预览快照，可在浏览器 dev server 里用真实曲库核对 UI（真实封面此前只有原生窗口能看到）。

### 新增（元数据修改与持久化）
- **用户可改写扫描出的元数据**（标题 / 歌手 / 专辑 / 专辑艺人 / 碟号 / 音轨号）：只要存在兜底推断就会有猜错，必须给用户改正的手段。入口是各页右键菜单与专辑页「…」的「编辑信息」，支持单曲与整张专辑。
  - 覆盖以**曲目 ID**（内容哈希）为键，扛得住文件移动、重命名、改标签；专辑 ID 是聚合派生的（改专辑艺人就变），绝不能当键，专辑级编辑因此在写入时展开成逐曲记录。
  - 只存被改过的字段：`None` 表示不覆盖，下次重扫读到更完善的标签时用户没碰过的字段仍会自动更新。界面也只提交用户真正动过的输入框，避免把「猜的」固化成「用户指定的」。
  - 字段三态：没动 / 清空（撤销该字段的修改）/ 改值——只有两态的话用户就只能整条还原，没法单独撤销某一栏。补丁类型（`TrackMetadataPatch`）与落盘类型（`TrackOverride`）分开建模：文本用空串表示撤销，数字用嵌套 `Option` 区分字段缺席与显式 null，否则碟号、音轨号只能改不能撤销。
  - **不写回音频文件**：写标签是破坏性操作，不该是「编辑信息」的副作用；修改只存在应用数据目录。
- 曲目与专辑新增**字段来源**标记（标签 / 目录 / 文件名 / 同专辑多数决 / 未知 / 用户修改），编辑对话框据此提示哪些信息是猜的、哪些已被改过。
- 扫描缓存 `ScanCache`：扫描的原始产出（不含封面字节，只留指纹）单独落盘，聚合与套用覆盖是纯内存计算。由此**改一次元数据不必重扫整库，重启也不必**——启动时读缓存 + 套覆盖即可还原曲库（实测 939 首恢复无需重扫）。
- 设置页的音乐文件夹改为显示后端的真实扫描目录与曲目数，不再是设计稿占位路径。
- 开发工具 `cargo run -p shannon-core --example warm_cache -- <目录> <缓存路径>`：预热缓存，用于端到端验证「重启免重扫」。

### 新增
- **窗口外观交还系统（macOS）**：此前窗口是个直角矩形，跟 macOS 上其他窗口摆在一起格格不入——关掉系统标题栏换来了自绘交通灯，代价是连系统圆角一起丢了。先补了套自绘圆角（透明窗口 + CSS），随后发现自绘的路走不到头：**绿灯远不止「最大化」**，hover 会展开窗口平铺面板（移动与调整大小 / 填充与排列 / 全屏幕），其中「排列」要摆布其他应用的窗口，是系统私有能力，自绘拿不到。
  - macOS 改用 `decorations: true` + `titleBarStyle: "Overlay"`：系统画窗口、交通灯浮在内容上，圆角、投影、双击标题栏缩放、边缘拖拽、平铺菜单全部回归原生。交通灯位置用 `trafficLightPosition` 对齐到侧栏，实测左 / 右 / 上三边等距（macOS 27 的按钮直径约 14、中心距 23，整组 62px；按 Big Sur 的 52px 去算会偏右 4px）。
  - Windows / Linux 保留自绘交通灯与自绘圆角（10px，接近 Windows 11 的 8px），外观本就仿 macOS，视觉不分叉。圆角外必须真透明，所以背景从 `body` 挪到 `#root`——`body` 的背景会铺满整个矩形视口，把圆角盖回去；最大化与全屏时去掉圆角（Windows 11 的最大化窗口就是直角）。浏览器预览不加圆角：那儿的窗口是浏览器画的，再切一圈只会在四角露出白色小三角。
- **绿灯的平铺菜单不再是英文**：那个菜单由 AppKit 提供，按应用声明支持的语言挑文案，没声明就退回英文——中文系统上照样弹英文菜单，跟 `src/i18n` 无关（那套管不到系统控件）。`src-tauri/Info.plist` 补上 `CFBundleLocalizations`（zh-Hans + en）。注意只在打包成 .app 后生效，`tauri dev` 跑的裸二进制没有 bundle，dev 下仍是英文。
- 歌曲页补上「播放全部」与「随机播放」：此前整页只能逐行点击，想从头听一遍全库没有入口。两处都有——头部与排序钮、过滤钮同排等高（挪到标题下方会顶高头部，拖窗口时标题跟着跳），吸顶栏因为还要塞下排序与过滤，只留图标圆钮。
  - **播放范围是当前过滤 + 排序的结果，不是整个曲库**：过滤出某位歌手的二十首之后按「播放全部」，用户要的是那二十首；与行点击的队列语义一致。列表为空时两个钮置灰。
  - 与专辑页 / 歌手页不同，这里的播放钮不做暂停态：那些页面能用「当前曲目是否属于本专辑 / 本歌手」判断「正在播的就是这一堆」，而歌曲页列的是全库，这个判断几乎恒真，按钮会长期显示成「暂停」，实际变成一个全局暂停钮——那是播放栏的职责。
- 收藏页的「歌曲」分段补上播放与随机（头部与吸顶栏均为图标钮；主头部在 1200px 以下进一步压缩间距与分段内边距，并把排序收成图标，保证英文 980px 下过滤框展开仍有可用宽度）。**只在「歌曲」分段出现**：那是唯一一个明确的曲目集合，专辑 / 歌手 / 歌单分段列的是容器，「播放全部」的含义并不明确——收藏了二十位歌手，点下去是要播八百首吗。
- 专辑 / 歌手 / 歌单详情页的吸顶栏补上随机播放：这三处吸顶栏原本只有播放钮，页面一滚下去随机播放就没得点了，而长专辑、长歌单恰恰是最需要它的地方。空歌单（刚建的还没加歌）时播放与随机一并置灰，不再往队列里塞空列表。
- 歌手页展开 / 收起的交互打磨：
  - 箭头改用 `chevronDown` 并旋转 180°——左右箭头在界面语汇里意味着「去往别处」，而这里是就地展开，什么都没跳转；旋转本身也是天然的过渡动作。
  - **收起时把区块标题带回视野**：一百多行收成十行，文档骤然变矮，浏览器会把滚动位置钳到新的底部，用户点了「收起」视线却被甩到别处。这是比动效更实际的体感问题。
  - 展开时只给紧邻的十来行做入场浮现（每行错开 15ms），其余直接显示；容器不做高度动画——`height: auto` 过渡要逐帧重排上百行，代价与收益不成比例。
  - 按钮文案交叉淡入、宽度平滑过渡（「显示全部 108 首」与「收起」宽度相差很大）。抽出可复用的 `ExpandToggle`，顺带补上 `aria-expanded`。
  - 全部遵从「减少动态效果」偏好。专辑区的横排 ↔ 网格切换保持瞬时：那些卡片带着 `layoutId`，加位移动画会重蹈封面弹跳。
- 歌手页的「显示全部」接入实现（此前两个按钮都没有行为）：热门歌曲与专辑区就地展开，热门歌曲的横向 5 行网格换成完整纵向列表、专辑横排换成自适应网格（`auto-fill`，不写固定列数），再点变「收起」。展开后播放队列跟着用当前可见的这一组，区块标题也从「热门歌曲」改成「歌曲」——列出的是按专辑顺序的全部曲目，不再是热度序。

### 变更（音频后端文档）
- 新增 [macOS 空间音频回放观测笔记](docs/MACOS_SPATIAL_PLAYBACK_NOTES.md)。此前空间音频只规划了 Windows 侧，三份研究笔记也全是 Windows 的，而 macOS 侧一片空白。本轮概念验证得到的主要结论：
  - **同一个 E-AC-3/JOC 文件，走错 API 就会静默跌落**：`AudioConverter` / `afconvert` 路径只解出 6 声道声床（对象丢失），`AVPlayer` + `MTAudioProcessingTap` 路径能拿到 12 声道（对象已渲染进 7.1.4）。两条都返回成功，没有任何错误提示。
  - **系统的杜比全景声标识由上游解码链路决定，不由交给渲染器的 PCM 决定**：同为 12 声道 f32 加同一布局标签，源是 JOC 时显示杜比全景声，源是普通 12 声道 WAV 时显示多声道。系统不会给确定无对象的内容标上全景声。
  - **应用可以在系统解码与系统渲染之间插入处理**：中转时施加 −6 dB，标识不变、空间定位正常、听感只是变轻。ReplayGain、音量斜坡这类处理因此可以保留在自己的管线里。
  - 代价是一条能力锁定：macOS 对象音频路径上**解码器被锁定为 AVPlayer**，换后端标识即消失。
  - 应用自行双耳化（`AUSpatialMixer` 渲染成 2ch 再输出）会让系统空间音频显示「不可用」，头部追踪也不可能有——渲染发生在头动之前。
  - 尚未确认：两条输出路径本身的听感是否等价。验证工具为独立仓库 `~/code/swift/SpatialCompare`，界面文案刻意中性以免污染听感判断。
- 架构约束与实现设计据此修订四处：分层与输出后端补入 macOS 空间路径；子进程红线由「不得有子进程」澄清为「不得以外部播放器作为播放兜底」（自身可执行文件的隔离 worker 在有明确崩溃隔离理由时允许，对应 Windows 侧 AC-4 OEM 解码器会拖垮进程的既有事实）；解码后端选择明确为运行时能力探测并记入 macOS 平台例外；实施阶段调整。
- 实施阶段的两处调整依据实测：**阶段 0 的格式主力由 FLAC 改为 ALAC**（真实曲库 1362 首中 ALAC 占 87.5%、FLAC 为 0，照原表会出现阶段 0 完成却一首曲库歌曲都放不了）；**声道布局建模提前到阶段 0**（多声道在立体声端点上必须正确下混，而下混系数依赖布局，事后补录代价大）。
- 依赖现场核实后落定原待定项：最低 Rust 版本 1.85、Symphonia 0.6.0（`mp3` / `isomp4` / `alac` / `aac` 均不在 default feature 内）、Opus 解码器选用 `symphonia-adapter-libopus`、空间输出明确不走 CPAL（其配置只有声道数，表达不了布局）。工程结构去掉 `crates/` 前缀，新成员与现有 `core/` 平铺——ts-rs 的 `export_to` 是相对路径，搬动目录会切断前后端契约链路。

### 修复
- **详情页找不到内容时不再是白屏**：专辑 / 歌手 / 歌单详情页原本都是 `if (!x) return null`，条件一旦成立整个组件不渲染，用户看到一片空白**且没有返回按钮**，只能靠侧边导航跳走，退不回原来的列表。这不是假想——重扫后专辑 ID 会变（它是聚合派生的），歌单可以在详情页开着的时候被删掉。改为统一的兜底页 `DetailNotFound`：说明发生了什么，返回按钮摆在与正常面包屑相同的位置上。
- 空歌单的空态不再显示成「歌单里没有“”」：那是「过滤没命中」的文案，被套用在了「歌单本来就空」的场景上——没有过滤词，引号里自然是空的，还引导用户去搜索，而此刻他需要的是知道怎么往里加歌。两种空态分开处理。
- 随机播放不再用 `sort(() => Math.random() - 0.5)`：那个比较器不满足排序要求（同一对元素每次比较可能给出不同结果），各引擎的实现细节会让某些位置的曲目出现概率明显高于其他位置——「随机播放」总是从那几首开始，用户是感觉得到的。四处调用点统一走 `src/lib/shuffle.ts` 的 Fisher-Yates。
- 修复歌手页展开全部歌曲后，右键“播放”与“显示歌词”仍使用热门歌曲短队列的问题；菜单操作现在与行点击共用当前可见队列，第 11 首之后不再发生索引错位。
- 专辑名里的数学符号（如 `∀rkadia` 的 `∀`）不再回落到风格迥异的系统字体：Lora 与 Noto Serif SC 都不含这类字形，字体栈中间插入 Times 风格的衬线数学字体，三平台各取所需（macOS 的 STIXGeneral、Windows 的 Cambria Math、Linux 的 FreeSerif / DejaVu Serif），让回落字形至少同属衬线调性。拉丁、中文、日文的度量完全不受影响。
- **客串歌手的歌手页不再是空白页**：歌手页原本只按「专辑艺人」聚合，而合辑、致敬盘里的演唱者多半从没当过任何专辑的专辑艺人（实测一张致敬盘的 10 位演唱者无一有页面），点进去整个组件直接不渲染——白屏且没有返回按钮，只能靠侧边导航逃出来。改为按「参与过的专辑 + 演唱过的曲目」聚合。
- 专辑详情页在曲目艺人不止一位时显示歌手列：设计稿是按「一张专辑一位歌手」画的，行里只有标题，但合辑、致敬盘、社团专辑里每首的演唱者都不同（实测 28 张里有 10 张如此，最多一张有 10 位），不显示就完全看不出谁唱的。单人专辑维持原样，每行重复同一个名字是噪音。歌手名可点击跳转其歌手页（默认与其他次要信息同色，hover 才提示可点，不抢标题的视线）。
- 歌手页收起歌曲时下方专辑封面会弹跳：封面的 `layoutId`（为卡片与详情页大封面的共享过渡而设）顺带开启了 layout 动画，于是列表长度突变造成的整体位移被逐张做成 spring 动画，低阻尼还会过冲。改用 `layoutDependency` 固定为专辑 ID，位置变化不再重新测量，共享过渡不受影响。
- 专辑 / 歌手 / 歌单详情页的吸顶栏补上返回按钮：页面滚动后面包屑已滚出视野，吸顶栏里没有返回入口，只能靠侧边导航跳走，退不回原来的列表。

### 修复（曲库聚合）
- 修复清空碟号或音轨号时被当成「字段未修改」、无法单独撤销数字覆盖的问题；数字补丁现在用字段缺席 / `null` / 数字分别表达没动 / 撤销 / 改值。
- **多碟专辑的曲目列表按碟分节**：此前用列表索引当序号，两张碟拼在一起就成了 1..30；现在按碟分组、组内显示真实音轨号，单碟专辑维持原样不显示碟标题。播放仍用全局位置，不受分组影响。
- **年份缺失不再显示「0」**：后端 `Album.year` 改为可选（判不出就留空，不用哨兵值），界面各处缺失时连同分隔符一起省略（`metaJoin`），专辑列表的年份列用破折号占位，按年份排序时无年份的排在最后。同一处的空 `genre` 造成的空档一并修掉。
- **多碟专辑不再按碟裂成多张**：碟名被写进专辑标签时（`Rebirth Story 5 Disc 1 SUN`），归组前先剥离碟号后缀，显示名取剥离后的主专辑名。只在独立词上匹配，`Discovery`、`CD Player` 不会误伤。
- **折叠同一张专辑内的重复曲目**，三条判据任一命中即视为同一首：
  - 「标题 + 时长（0.01 秒）」——副本的音频内容相同但元数据差几十字节，文件大小不同、内容哈希 ID 也就不同，光靠 ID 去不掉。
  - 「去掉末尾译名括注后的标题 + 时长」——补齐 `Winter Alice` / `Winter Alice（冬日爱丽丝）` 这类标题主体相同、译名有无不一且轨位标签互相冲突的副本；括注前的 mix / rearranged 等版本标识仍保留。
  - 「碟号 + 音轨号 + 时长」——同一首歌常被标上不同标题（实测同一轨位有 `ロミオとシンデレラ`、`…（罗密欧与仙杜瑞拉）`、`…（罗密欧与灰姑娘）` 三种写法），只比标题认不出。同一张专辑的同一轨位就是同一首歌；再要求时长一致，防的是标签把两首不同的歌错标到同一轨位。
  - 同一录音的副本若带着互相冲突的轨位，先按组内多数决定碟号与音轨号，再优先保留轨位一致且信息更全的那份。跨专辑的同名曲目、同轨位但时长不同的曲目都不受影响；折叠数如实上报（`LibrarySnapshot.duplicates`）。
  - 实测该库 939 个文件里有 608 首重复拷贝，折叠后为 331 首；`doriko BEST 2008-2016` 正确收敛为 2×15 共 30 首，另一张同样存在译名副本的 tribute 专辑由 20 首收敛为 10 首。
- **缺封面的零散曲目能并回主专辑**：整张专辑好好待在一处、另有几首缺封面又缺标签的文件搁在别的艺人目录下时，封面证据用不上，改用「恰好一方无封面 + 目录名相同」作为退路。两边都没封面则保守分开（可能只是两位歌手各有一张同名专辑）。实测 doriko BEST 2008-2016 的两首漏网曲目因此并了回去，不再是两张各 1 首的无封面假专辑。
- **合辑不再被拆成十几张专辑**。根因是无专辑艺人标签时逐曲回落到曲目艺人；改为按组判定：有专辑艺人标签用标签，没有则组内多数决，高度分散才判为合辑（`Various Artists`）。实测一张 116 首、横跨 6 个艺人目录的合辑此前被拆成 7 张。
- 同一张专辑散在多个目录时按**封面指纹**合并：合辑成员常按曲目艺人分目录存放且只有主创目录写了专辑艺人，目录与音轨号都认不出它们同属一张，但封面是同一张图。封面**只作合并证据、不作拆分依据**——一张专辑内部可能有几首嵌了不同版本封面，当归组键会把好好的专辑劈成两半。
- 专辑归组不再只看「专辑艺人 + 专辑名」，避免两位歌手各自的《Greatest Hits》撞成一张。
- 扫描根目录不再被当作专辑名或歌手名：散落在库根目录的文件曾被聚成一张以库目录命名的假专辑，歌手则变成上一级目录名。
- 扫描进度事件的曲目数改为实时上报（此前恒为 0，只有收尾那一帧才有真值）。
- 修复 `src/data/playlists.ts` 引用已改名导出（`ALBUMS` / `allTracks`）导致的类型检查失败。
- 为 `ts-rs` 启用其官方提供的 `no-serde-warnings` feature：Serde 的 `skip_serializing_if` / `deserialize_with` 仍按原语义工作，TypeScript 可选字段继续由显式 `#[ts(optional)]` 建模，同时不再为这些已知不支持的属性输出误导性构建警告。
- 生产构建按 React 与 Framer Motion 运行时拆分稳定缓存块，保留歌词引擎原有的页面级懒加载边界；避免把全部依赖合成单一 vendor 包，构建产物不再出现单块超过 500 kB 的警告。
- 实测：939 个文件的真实曲库，专辑数由 41 张收敛到 28 张（无一张被误拆），曲目折叠重复后为 331 首。

### 新增（Rust 后端 · 曲库扫描）
- 新增 `core/`（`shannon-core`）crate：曲库扫描、音频规格探测、稳定 ID。**刻意不依赖 Tauri 与任何 GUI 库**，因此能在无图形环境下 `cargo test`，也让 Tauri 外壳退化为一层薄适配（进度上报以回调注入）。
- 前后端契约由 ts-rs 从 Rust 结构体生成到 `src/types/generated/`：Rust 结构一改前端就编译不过，与 i18n 的 `Messages` 接口同一理念。
- 音频规格模型 `AudioFormat`：容器 / 编解码 / 编码族（PCM · DSD）/ 采样率 / 位深 / 比特率 / 无损 / 声道数 / **声道位掩码** / 具名布局 / **空间音频标记** / 探测备注 + 探测器版本。
  - 声道数不足以判定布局：6 声道可能是 5.1 也可能是 6.0（摆位不同下混系数就不同），故以掩码为权威、具名布局仅作投影，判不出一律留空而不硬猜。
  - 空间音频与声道维度正交独立建模：Atmos 的声道数可能报 5.1 甚至 2，真正的信息在容器的对象元数据里。
  - DSD 的采样率语义不同（DSD64 = 2 822 400 Hz / 1 bit），由编码族字段区分展示口径。
  - `probeVersion` + `probeNotes` 为日后增强探测留口：探测器升级后可只重扫版本落后的条目。
- 探测分工：`lofty` 读标签与内嵌封面，`symphonia` 提供声道掩码与采样参数，容器级模块单独处理 DSF header 与 MP4 的 `dec3`（E-AC-3 JOC）/ `dac4`（AC-4）。
- **识别与播放能力解耦**：扫描只负责如实记录，不因暂时播不了就跳过文件——那等于告诉用户「你没有这些歌」。解析失败的文件计入 `failed` 如实上报。
- 稳定曲目 ID：以「文件大小 + 跳过元数据区后的三段内容 blake3 + 格式指纹」为键，文件移动、重命名、改标签都不会断开收藏与歌单关联。
- 标签缺失时按 `歌手/专辑/曲目` 目录约定兜底归组，而非把整盘未标签文件并进「未知专辑」。
- 前端曲库改由 `useLibraryStore` 承载，组件经 `src/lib/library.ts` 访问「生效曲库」：无后端或未扫描时回落到种子数据（否则没有音乐文件夹时界面全空，UI 无法开发），扫描完成后整库替换并强制重挂载各页。
- 首次启动引导接入真实扫描：选择文件夹 → Rust 后台扫描 → 进度、当前文件路径、曲目 / 专辑数均由事件回灌，替换原 180ms 模拟时钟；扫描失败会在界面上明示而非静默返回欢迎页。

### 新增
- 设计参考资料：整理「香农播放器设计简报」离线导出包到 `docs/design/`。
- Windows 音频后端研究文档：整理 Atmos/JOC 系统解码与 7.1.4、9.1.6、22.2 固定多声道
  空间回放概念验证，记录布局识别、流式读取、系统空间路由、可观测指标和结论边界。
- 音频后端架构约束：实时播放链保持进程内运行，子进程只用于用户安装的 `yt-dlp` 与必要的
  离线导入处理；明确纯 Rust 主解码路径、可选进程内后端和禁止隐藏播放兜底。
- 音频后端实现设计：定义 cargo workspace 拆分、控制面/数据面线程模型、current/next
  队列交接、异步一致性标识（generation / queueRevision / request_id）、Symphonia
  插件式解码、前端引擎适配层、无头测试策略与六个实施阶段。
- Tauri 2 + React 19 + TypeScript + Vite 工程骨架。
- 设计 Token 层：Tailwind v4 + CSS 变量，浅色 / 深色两套语义配色。
- 曲库主界面（「杏色 · 明快 2a」设计）：
  - 图标栏：导航、语言菜单、浅色 / 深色 / 跟随系统三态外观切换。
  - 专辑网格视图（封面 hover 浮现播放键）与详情列表视图，可切换。
  - 悬浮播放条：播放 / 暂停、上一首 / 下一首、随机、循环、进度、音量、收藏。
  - 自绘 macOS 交通灯，接入真实窗口控制（关闭 / 最小化 / 最大化）。
  - 弹性滚动物理与自绘滚动条。
  - Radix UI 右键菜单（键盘导航）与语言下拉菜单。
- 强类型播放器领域模型：播放队列、播放进度、音频设备、循环模式（`src/types/player.ts`）。
- 类型安全的国际化：当前提供简体中文与 English，界面文案实时切换（i18n 架构可扩展更多语言）。

### 新增（歌曲页）
- 歌曲页（侧边栏「歌曲」入口的全曲库长列表，依据设计稿逐项复刻）：副标题实算总数与总时长、吸顶栏（滚动超阈值弹入，含过滤框）。
- 排序菜单：按标题（拼音，界面用 localeCompare('zh')，后端将换 pinyin crate）/ 按歌手（分组视图：歌手标题 + 专辑小节 + 组内编号）/ 最近添加。
- 过滤圆钮：hover 从圆形展开为输入框（40→318，吸顶栏 34→300），聚焦强调圈、命中文字 --ac 高亮、Esc / 清空按钮关闭、无结果空态。
- 行为与其他页一致：点击行以当前可见列表替换队列、当前行均衡器、行内爱心、曲目右键菜单（播放 / 下一首播放 / 收藏 / 查看歌词）。

### 新增（歌词页 · AMLL）
- 歌词页（沉浸式，覆盖整窗）：歌词渲染接入 AMLL（applemusic-like-lyrics），逐字填充、行间弹簧滚动、远行降透明与虚化、间奏指示均由 AMLL 提供；设计稿中的逐行模拟仅作布局参考。
- 页面 chrome 按设计稿复刻：左栏 300px 封面（主色氛围光 + 材质投影 + hover 收藏）、迷你控制簇、进度与音量；右上工具簇（译 / 音 / Aa / 设置，鼠标静止 3s 自动隐藏）；右下歌词开关与队列按钮；磨砂队列面板（随机 / 循环、继续播放、清除）。
- 「译」开关切换 AMLL 译文行；隐藏歌词时左栏铺满为沉浸「正在播放」视图。
- 无歌词曲目显示空态（在线查找 / 导入 .ttml / .lrc 占位入口，来源注记 AMLL TTML DB）。
- 歌词种子：虚构歌曲「午夜环线」原创逐字歌词（文本来自设计稿）；领域模型 LyricLine 增加逐字时间轴。
- 模拟播放时钟：播放中进度实时推进，曲尾按循环模式自动切曲 / 停止（真实音频引擎接入前的占位）。
- 入口：播放条左侧曲目区点击、曲目右键菜单「查看歌词」。

### 新增（歌手详情页）
- 歌手详情页（依据设计稿逐项复刻）：圆形头像（主色调投影，hover 浮现收藏爱心）、收藏角标、播放全部 / 随机播放。
- 热门歌曲：双列五行横向滚动网格（带专辑列、当前行均衡器、行内爱心、曲目右键菜单）。
- 专辑横排卡片：hover 浮现收藏与播放按钮、已收藏角标、点击进入专辑详情页。
- 吸顶栏：滚动超过阈值后弹入（头像 + 歌手名 + 播放键）。
- 专辑详情页的歌手名可点击进入歌手页；播放器 store 新增歌手收藏映射。
- 曲库种子补齐白鲸电台另外 6 张专辑（空港日记 / 白鲸电台 / 深海信号 EP / 环线现场 / 夜航 / 白噪音练习）及部分真实曲目名。

### 新增（专辑详情页）
- 专辑详情页（依据设计稿逐项复刻）：面包屑返回、大封面（材质内描边 + 封面主色调投影，hover 浮现收藏爱心）、已收藏角标、元信息行、播放 / 随机播放 / 更多按钮。
- 曲目列表：当前播放行显示三柱跳动均衡器（暂停时定格）、行内爱心常驻可点、曲目右键菜单（含「查看歌词」项）。
- 点击曲库中的专辑卡片 / 列表行进入详情页；封面 hover 播放键与详情页播放均以整张专辑替换播放队列。
- 播放器 store：新增 playQueue、曲目 / 专辑收藏映射；切换曲目时进度与时长正确重置。
- 曲库种子加入设计稿虚构专辑「长夜电波 — 白鲸电台」（含 10 首曲目），各专辑补充流派字段。
- 专辑 / 曲目右键菜单接线：播放、下一首播放（保持专辑内顺序插队）、收藏。

### 新增（歌单详情页）
- 歌单详情页（依据设计稿逐项复刻）：2×2 拼贴封面（四象限专辑封面，hover 浮现收藏爱心）、播放列表角标、已收藏角标、描述与元信息（首数 / 总时长 / 更新时间）、播放 / 随机播放 / 更多 + 过滤圆钮。
- 曲目列表：当前播放行三柱均衡器、序号 hover 变拖拽手柄（静态示意）、跨专辑曲目带专辑列、行内爱心、命中文字高亮、无结果空态（引导在整个曲库中搜索）。
- 吸顶栏：滚动超阈值弹入（迷你拼贴 + 歌单名 + 收藏角标 + 过滤框 + 播放键）。
- 曲目右键菜单：播放 / 下一首播放 / 收藏 / 查看歌词；末项为「从歌单移除」（区别于曲库的「从资料库移除」）。
- 歌单入口：初版经收藏页临时卡片网格进入，现由正式收藏页承接（见下）。
- 歌单种子数据「深夜驾驶」（12 首跨专辑曲目，封面取前 4 首拼贴）；领域模型新增 Playlist 类型；播放器 store 新增歌单收藏映射。
- 通用组件：2×2 拼贴封面 Collage、共享过滤圆钮 FilterPill（从歌曲页抽出复用）、拖拽手柄 grip 图标。

### 新增（收藏页）
- 收藏页（依据设计稿逐项复刻）：标题 + 收藏统计副标题、分段切换、排序菜单（仅歌曲：最近收藏 / 按标题 / 按歌手）、过滤圆钮、吸顶栏（滚动弹入，含分段与过滤）。
- 分段直接映射播放器收藏状态，点击行内 / 卡片爱心即时取消收藏并从列表消失；各分段独立空态与过滤无结果态。
  - 歌曲：曲库收藏曲目表（当前行均衡器、命中高亮），按歌手排序时切换为「歌手 → 专辑小节 → 组内编号」分组视图。
  - 专辑：复用曲库卡语言，hover 浮现取消收藏 + 播放，标题旁已收藏角标，点击进入专辑页。
  - 歌手：圆形头像（取最新专辑封面），hover 浮现取消收藏，名旁收藏角标，点击进入歌手页。
- 设计稿为三分段（歌曲 / 专辑 / 歌手）；因歌单为可收藏实体、需要归处，此处扩展「歌单」为第四分段（收藏的歌单卡片网格），作为歌单页的正式入口，替换先前临时卡片网格。
- 种子补充：收藏歌手（白鲸电台 / 万能青年旅店 / 陈绮贞 / Radiohead）以对齐设计稿演示态。

### 新增（搜索页）
- 搜索页（依据设计稿逐项复刻）：居中搜索框（聚焦强调圈 + 清空按钮）、范围筛选（全部 / 歌曲 / 专辑 / 歌手 / 歌单）、结果按分组呈现、匹配文字琥珀高亮。
- 真实检索本地曲库：歌曲（标题 / 歌手命中，缩略图 + 当前播放行均衡器）、专辑（卡片）、歌手（圆形头像）、歌单（列表行）；空查询显示最近搜索，无结果有空态。
- 结果点击直达：歌曲替换队列播放，专辑 / 歌手 / 歌单进入对应详情页。
- 入口：图标栏新增「搜索」项（对齐搜索页设计稿的图标栏），⌘F / Ctrl+F 全局唤起，曲库主界面搜索框亦接线跳转；NavKey 增加 search。
- 最近搜索存于 UI store（会话内，回车提交时前插去重、上限 8 条），后期由后端持久化。

### 新增（设置页）
- 设置页（依据设计稿逐项复刻）：曲库 / 播放 / 歌词 / 外观与语言 / 关于五个分区，右侧目录随滚动高亮、点击平滑跳转。
- 曲库：音乐文件夹卡片（路径 + 曲数 · 状态，可移除）、「添加文件夹…」占位入口；实时监听 / 云盘占位符检测开关。
- 播放：响度均一化开关（默认关，仅增益不压缩）。歌词：在线歌词源 / 逐字高亮开关。
- 外观与语言：外观三态分段（浅色 / 深色 / 系统，直接驱动主题 store，与图标栏共享状态）；界面语言下拉（仅承诺简体中文 + English，沿用 LANGUAGES）。
- 关于：应用图标 + 名称 + 版本 / 协议 / 免费声明，源代码 / 支持者名单 / 打赏支持按钮。
- 开关与文件夹状态存于 UI store（会话内），新增 setTheme 直接设定外观；nav==="settings" 改路由到 SettingsScreen（懒加载）。

### 新增（首次启动页）
- 首次启动引导（依据设计稿逐项复刻）三态流程：欢迎（空曲库）→ 扫描（旋转唱片 + 进度条 + 实时曲数/专辑数 + 当前文件路径，可后台继续 / 取消）→ 完成摘要（专辑拼贴 + 统计 + 开始听歌）。
- 引导期间覆盖主内容区、隐藏播放条，图标栏保持可用（对齐「全量扫描异步执行」）；「开始听歌」结束引导并回到专辑页。
- 入口：由设置页「添加文件夹…」唤起（onboardingOpen 状态，默认关）；后期由后端根据曲库是否已配置，在真实首次启动时自动触发。
- 扫描进度为占位模拟时钟（180ms 递进），真实扫描进度接入后端后回灌。

### 新增（歌手列表页）
- 图标栏「歌手」入口的歌手索引页（设计简报未覆盖，按现有设计语言补齐）：圆形头像网格（取歌手最新专辑封面）+ 歌手名 + 专辑数，按拼音排序，点击进入歌手详情页；替换原占位符。
- 复用收藏页 / 搜索页已确立的圆形头像卡语言与曲库主界面标题栏（标题 + 计数副标题 + 搜索入口）。

### 新增（歌单列表页）
- 图标栏新增「歌单」入口与歌单索引页：列出全部歌单（含未收藏与新建的），2×2 拼贴封面卡 + 标题 + 元信息，已收藏带角标，卡片 hover 可直接收藏 / 取消收藏，支持最近更新 / 按标题 / 按曲目数排序与页内过滤，空态区分「无匹配」与「还没有歌单」。
- 歌单支持拖拽自定义顺序：歌单是用户内容，顺序应由用户决定（专辑等曲库内容不提供）。从任意排序模式起拖会先固化当前可见顺序再切到「自定义顺序」，过滤中禁用拖拽。网格会换行，framer-motion 的 Reorder 只处理单轴，故按指针落点判定目标格位、其余卡片以 layout 动画补位。排序模式存入 UI store 而非组件局部 state——否则离开页面再回来会重置为「最近更新」，用户手动排的顺序被静默丢弃。
- 修复歌单取消收藏后无处可寻：此前歌单只有「收藏页 → 歌单分段」一个列表入口，且只显示已收藏的，取消收藏后仅能靠搜索名称找回；「新建歌单并加入」创建的歌单默认未收藏，等于一建出来就从界面消失。根因是把「收藏」当成了歌单的存在条件——歌单是用户创建的内容，不同于专辑 / 歌手可以退回曲库找到。现由歌单页承担「全部歌单」，收藏页的歌单分段回归本义（已收藏的歌单子集）。

### 新增（加入歌单）
- 歌单数据入 store：`PLAYLISTS` 种子迁入播放器 store 的 `playlists` 状态，歌单详情 / 收藏 / 搜索页改从 store 读取，加入曲目后各处实时更新；新增 `addToPlaylist`（按曲目 ID 去重，重复加入为 no-op）与 `createPlaylistWithTracks`（重名自动加序号）两个动作。
- 播放条「加入歌单」按钮接入真实菜单：上方弹出歌单列表（缩略拼贴封面 + 曲数，已含当前曲目的歌单显示对勾），底部「新建歌单并加入」一键建单；按钮呈激活态。
- 曲目 / 专辑右键菜单的「加入歌单…」改为子菜单：列出全部歌单（已含该曲目的显示对勾）+「新建歌单并加入」，覆盖歌曲页、专辑 / 歌手 / 歌单详情页、收藏页与专辑网格 / 列表（专辑级为整张加入）。
- 新建歌单默认名「新歌单」（i18n），手动建单的更新时间展示为「今天更新」。

### 变更
- 详情页「…」按钮接入实现（此前两处都只有样式、没有 onClick）。两者内容按所有权区分：
  - 专辑「…」= 专辑卡右键菜单本身（专辑属曲库内容，可做的动作就是 `ALBUM_MENU` 那些，不额外发明）；右键是隐藏交互，「…」是它的显式鼠标入口。为此把菜单项渲染抽成可复用实现，右键版与下拉版共用，内容不会分叉。
  - 歌单「…」= 歌单的管理动作：重命名… / 下一首播放（整单插队）/ 把本歌单加入…（并入其他歌单或新建）/ 删除歌单（danger）。
- 补齐歌单的重命名与删除：此前新建的歌单叫「新歌单」永远改不了名，建出来也没有任何途径删除——给了创建没给管理。删除走二次确认，且明确告知歌曲不会从曲库移除；删除同时清掉该歌单的收藏标记，避免留下孤立键。
- 新增模态基座 `src/components/common/Modal.tsx`（Radix Dialog）：`PromptDialog` 文本输入（自动聚焦全选、回车提交、空值不可提交）与 `ConfirmDialog` 破坏性二次确认，沿用菜单的表面材质与 spring 弹出曲线，遵从减少动态效果偏好。
- 全局播放快捷键（`src/hooks/useGlobalHotkeys.ts`）：空格播放 / 暂停、`←` `→` 快退快进 5 秒、`⌘/Ctrl + ←/→` 上一首 / 下一首、`↑` `↓` 音量 ±5%、`M` 静音、`⌘/Ctrl + F` 唤起搜索；焦点在输入框或菜单内时整体让位。设置页「播放」新增只读的快捷键速查表（自定义键位需持久化，留待后端）。
- 歌单曲目支持拖拽重排：拖拽柄此前只有视觉（`cursor-grab` + hover 显示 grip + 提示文案）而无实现，现按住柄即可排序；过滤中列表是子集、重排语义不明确，此时关闭拖拽并隐藏柄。
- 队列面板补齐单项操作：整行可拖拽重排「接下来」、单击跳到该曲播放、hover 显示移除按钮（当前播放项不可移除）——此前整行有 `cursor-pointer` 却没有任何点击行为。
- 曲目右键菜单「从歌单移除」接入实现（此前点击无反应）。
- 头部元信息副标题不再折行：标题列改为不参与压缩（页面身份优先，空间压力仍由过滤钮吸收），并新增 `MetaLine` 组件把「3 首 · 2 张专辑 · …」的每个片段包成不可拆整体——极窄场景下只能断在分隔符处，不会出现「1」与「playlists」被拆到两行的孤字断行。此前英文下收藏页副标题在 980 / 1180 均会折成两行、专辑页副标题在 980 折行。
- i18n 支持单复数选词 `{var|one|other}`：英文所有「数字 + 名词」文案改用该标记，修正「1 playlists」「1 albums」这类错误；中日文无单复数变化，不受影响。同时补上简繁中文收藏页副标题缺失的量词（「1 歌单」→「1 个歌单」）。
- 专辑页搜索与其他页统一：原「搜索」按钮（跳转全局搜索页）改为与歌曲页 / 歌手页 / 收藏页 / 歌单页一致的页内过滤钮，hover 展开输入框，按专辑名或歌手名过滤，网格与列表视图共享结果，无命中时显示空态；全局搜索仍由图标栏入口进入。
- 头部控件在窄窗口不再换行：排序钮与分段控件改为不参与压缩且强制单行（此前英文「Recently favorited」在 980 / 1180 宽度下会被挤成两行），改由过滤钮作为头部唯一可收缩项让位（收起态仍是固定圆钮，展开态可收缩至 132px 下限）。[980×640, ∞) 实测零横向溢出。
- 设置页右侧目录不再因窗口宽度而消失：原 `xl`（1280px）断点以下整栏隐藏，导致设计稿默认尺寸 1180 下看不到目录。改为「内容列 680 + 目录栏 220」组成 900 版心整体居中，目录与正文保持固定 49px 间距，在 [980, ∞) 全区间常驻。
- 桌面外壳手感：界面文本默认不可选中（补 `-webkit-user-select` 前缀以适配 WKWebView / WebKitGTK，仅输入类控件保留可选中以支持选词 / 粘贴）；在 Tauri 原生窗口内兜底拦截浏览器右键菜单（重新加载 / 检查元素等非预期项），仅输入类控件放行原生复制 / 粘贴，应用自身的 Radix 右键菜单不受影响；浏览器 dev 环境不拦截，保留原生右键便于调试。
- 播放条队列按钮接入真实队列面板：与歌词页共享同一磨砂浮层组件（随机 / 循环圆钮、接下来列表、行内爱心、清除），从播放条右上方弹出，点面板外或 Esc 关闭，按钮呈激活态；歌词页队列面板改为引用共享组件并同样支持点外关闭。
- 播放条「加入歌单」图标由设计稿自造隐喻（上箭头 + 圆 + 底线，可读性差）改为通用的「列表 + 加号」，与相邻队列图标（三横线）视觉同基因，无需悬停提示即可读出语义。
- 播放条与歌词页的音量控制改为共享原生滑杆：保留 4px 细轨道视觉，同时扩大到 24px 高命中区，交互时浮现竖向圆角矩形手柄，支持连续拖动与方向键微调；音量图标可单击静音并保留原音量，再次单击恢复，播放条有效长度同步增加。
- 播放控制簇补齐语义动效：随机路线交错重组，上一首 / 下一首沿跳轨方向压缩位移，循环状态以半周旋转衔接并在单曲循环时显示动态 `1` 标识；播放条与歌词页共用同一套固定占位控件。
- 播放操作视觉升级：主播放键增加克制的哑光内高光、边界与按压深度；播放 / 暂停符号改为光学居中的圆角几何，并通过同拓扑 SVG 路径实现连续形变，统一覆盖播放条、歌词页、详情页、吸顶栏与封面播放入口。
- 动效系统完成 P1/P2/P3 收口：Radix 菜单与歌词队列补齐退场，歌词栏同步展开收起，收藏 / 播放 / 序号 / 均衡器 / 拖拽柄统一交叉淡化或弹簧反馈；收藏移除、卡片筛选重排、首启阶段、空态与搜索分组平滑过渡，专辑网格封面可共享元素过渡至详情大封面，播放进度按 100ms tick 平滑衔接。为避免 200+ 行曲库的布局计算开销，长歌曲列表筛选仍保持即时更新，仅收藏移除行启用补位动画。
- 窗口自适应：专辑网格（曲库主界面与收藏页专辑分段）由固定 4 列改为 auto-fill 随宽度增减列数（1180 设计稿尺寸仍为 4 列，980 下限 3 列，1920 约 7 列），超宽窗口下封面不再被撑到失衡；歌词页封面尺寸随视口高度收敛（最矮 640 时约 230px），左栏音量条不再贴底。全页面在 [980×640, ∞) 区间实测零横向溢出，布局戒律记入 CLAUDE.md / AGENTS.md。
- 歌曲页按歌手排序时，歌手名称与分组统计会随滚动吸顶，并停靠在歌曲页主吸顶栏下方；下一位歌手进入时自然替换。
- 歌曲页、收藏页与专辑列表加入内缩、低对比度的行间细分隔线：平铺和按歌手分组视图均从标题列开始，保留既有 hover 与当前播放强调；专辑列表同步改为无框表格语言。
- 主内容页切换加入轻量横向淡入淡出过渡：侧栏导航、专辑 / 歌手 / 歌单详情、首次启动引导与全屏歌词页均使用同一机制，侧栏与播放条保持稳定。
- 专辑详情页补齐与歌手、歌单一致的滚动吸顶栏：保留缩略封面、专辑与歌手信息、收藏标识及播放 / 暂停，不重复放入筛选或次级操作。
- 专辑主界面的标题栏改为随内容滚动，并在滚过后以精简吸顶栏保留专辑标识、网格 / 列表切换、排序与全局搜索操作。
- 歌手主界面同样改为标题随内容滚动，并以吸顶栏保留歌手标识、排序与页内过滤。
- 歌曲页吸顶栏补齐排序菜单，与标题栏共用排序状态；过滤展开时会推开排序控件。
- 白色底座式分段选择统一为弹簧滑移动画：专辑网格 / 列表、收藏分类及外观主题切换均按选项实际宽度平滑过渡；专辑视图与收藏分类内容同步淡入淡出，且均遵从系统的减少动态效果偏好。
- 专辑页新增最近添加 / 标题 / 歌手排序，网格与列表视图共享顺序；歌手页新增名称 / 专辑数排序。
- 收藏页排序扩展到全部分段：专辑可按标题或歌手排序，歌手与歌单可按标题排序；切换分段时自动保留或恢复适用的排序项。
- 专辑封面与中大型表面容器改用连续圆角（superellipse）：保留各组件原有圆角尺度，令直线与弧线更平滑地衔接；不支持该 CSS 特性的环境自动回退为原有圆角。
- 滚动：手感交还各平台原生（macOS 触控板橡皮筋、Windows/Linux 滚轮惯性各自沿用系统实现，最贴合本机肌肉记忆），只统一视觉——隐藏系统滚动条、改绘跨平台一致的自绘 thumb（按原生滚动位置直接映射，静止 0.9s 后淡出）。曾评估过自积分速度 + 橡皮筋的自定义引擎，因难以在各平台/输入设备上都还原原生手感，权衡后回退。
- 衬线字体（Lora / Noto Serif SC 可变字重）改为随应用本地打包，移除对 Google Fonts CDN 的运行时依赖，离线可用。
- 专辑详情页、歌手详情页、歌曲页、歌词页改为按需加载，AMLL 渲染依赖不再进入首屏主包。
- 依据设计稿核对并对齐曲库主界面细节：播放条爱心常驻强调色（实心 = 已收藏，描边 = 未收藏）、进度与音量槽圆角、滚动条圆角、图标栏按压时长与标识边距、封面 hover 罩层时长。
- 补齐设计 Token `--pbk`（深墨播放条底，供歌词页等场景使用）。

### 修复
- 歌单种子优先复用曲库规范曲目 ID，避免同一歌曲因演示数据 ID 不同而绕过去重或漏掉
  「已在歌单中」标记；新建歌单会自动加入收藏页，且同批输入中的重复曲目只保留一次。
- 更正 Windows 22.2 概念验证中的 LFE 合并说明：单路有信号时保持 unity gain，仅在两路
  同时有信号时应用条件衰减，并补记对应自检与空间提交回归。
- 短窗口下图标栏会自动收紧项目间距与上下留白，保留导航文字、语言与外观控制的完整可见和既有点击面积。
- 歌词页在短窗口为左上返回区预留顶部安全区，避免其与左栏封面重叠。
- 过滤圆钮收起时对 40px 搜索图标作光学校正，使放大镜镜片中心与按钮中心对齐。
- 设置页滚动内容底部预留与其他页面统一为 120px，避免播放条遮挡「关于」区末尾操作。
- 各页面展开式过滤框改为参与 flex 布局，展开时会推开左侧控件，不再覆盖排序、分段等操作。
- 专辑封面 hover 外投影改为独立透明度图层，避免触控板滚动时指针掠过封面触发 box-shadow 重绘而掉帧。
- 歌手列表页搜索入口改为页内过滤歌手网格，不再错误跳转到全局搜索页；补齐过滤提示与无结果状态。
- 专辑封面播放键的 aria-label 由硬编码中文改为 i18n 插值文案。
- 专辑卡片 hover：去掉 scale 缩放（非整数缩放会重采样已光栅化的圆角、导致边缘毛糙），仅保留抬升；hover 时抬高卡片 z-index，使升起的卡片与投影不再被相邻卡片遮挡（曲库网格与歌手页专辑横排同修）。

### 说明
- 尚未接入真实音频播放与本地曲库扫描；曲库为演示种子数据。

[未发布]: https://example.com/shannon-player/compare/HEAD
