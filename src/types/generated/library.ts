// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AudioFormat } from "./audio";

/**
 * 专辑（由曲目聚合而来，非独立实体）。
 */
export type Album = { id: string, title: string, artist: string, year: number, genre: string, cover: Cover, trackCount: number, durationSec: number, };

/**
 * 封面：占位渐变（首字母）或真实图片。与前端 `Cover` 对齐。
 */
export type Cover = { initial: string, 
/**
 * 渐变起止色。
 */
gradient: [string, string], 
/**
 * 真实封面图 URL（有则优先）。
 */
url?: string, };

/**
 * 一次扫描的产出。
 */
export type LibrarySnapshot = { albums: Array<Album>, tracks: Array<Track>, 
/**
 * 遍历到但无法解析的文件数（不静默丢弃，如实上报）。
 */
failed: number, };

/**
 * 扫描进度事件（Tauri event `library://scan-progress`）。
 */
export type ScanProgress = { 
/**
 * 已处理文件数。
 */
done: number, 
/**
 * 已发现的候选音频文件总数（遍历完成后才确定）。
 */
total: number, tracks: number, albums: number, 
/**
 * 当前正在解析的文件路径（首启页滚动展示用）。
 */
current: string, };

/**
 * 曲目。
 */
export type Track = { id: string, title: string, artist: string, album: string, albumId?: string, cover: Cover, durationSec: number, 
/**
 * 本地文件绝对路径。
 */
path: string, 
/**
 * 碟号 / 音轨号（用于专辑内排序）。
 */
discNo: number | null, trackNo: number | null, format: AudioFormat, };
